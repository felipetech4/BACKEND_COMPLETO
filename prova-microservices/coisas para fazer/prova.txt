mapeamento das api:
	cadastro-ms:
			  |
			  |
			  |
			  |
			  |
			  |
			  |
	venda-ms:
		POST  | adicionar uma venda       | venda-ms/api/venda/adicionar
			ex:
				{
    				"codigo": "{codigo do produto a ser vendido}", // @NotEmpty @NotBlank
    				"quantidadeVendida": 1, // min (1)
    				"dataVenda": null // pode ser passado uma data (dd/MM/yyyy) ou não (considera data atual), se for passado qualquer diferente de (null, "", " ") 
					sera considerado data invalida
				}
		GET   | exibir todas as vendas por codigo  				  | venda-ms/api/venda/pesquisar-por-codigo/{codigo}
		GET   | exibir todas as vendas com produto 				  | bota a url aqui felipe
		GET   | exibir vendas por periodo			 			  | venda-ms/api/venda/pesquisar-por-intervalo/{data1}/{data2} ex:31-11-2001
		GET	  | exibir venda unica por id			 			  | venda-ms/api/venda/{id}
		GET	  | exibir todas as vendas sem produto  			  | venda-ms/api/venda
		DELETE| cancelar uma venda (produto volta para estoque)   | venda-ms/api/venda/cancelar/{id} 
		DELETE| deletar uma venda (produto não volta ara estoque) | venda-ms/api/venda/deletar/{id}

funções: 
	cadastro-ms:
 		pesquisar produto por id = OK
		listar todos os produtos = OK
		incluir produto = OK
		alterar estoque = OK (adicionar e remover)
		excluir produto = OK
		
	venda-ms:
		realizar venda = OK
		buscar venda por id = OK
		listar todas as vendas por codigo = OK
		listar todas as vendas (sem os produtos) = OK
		listar vendas por periodo = OK
		Hystrix adicionado a venda = Ok
		
	opcionais:
		fazer um config em cadastro-ms e venda-ms pra tirar o classpath do mongo = OK
		excluir venda = OK
		cancelar venda = OK (falta adicionar no estoque)
		alterar preco de produto = OK (fiz um put geral)
		listar todas as vendas (com os produtos) opcional (felipe)
		adicionar um atributo total em venda = em progresso (gabriel)
		
validações:
	ver se quantidadeVendida < quantidadeEstoque = OK
	todos os input de cadastro e venda = OK


repositorio atual no mongo: Desafio
collections: produto, venda



lembrar de mexer na properties de config-server

DUVIDAS:
método get pesquisar por intervalo como usar?
/venda-ms/api/venda/pesquisar-por-intervalo/{data1}/{data2}
/venda-ms/api/venda/pesquisar-por-intervalo/31-04-2022/02-11-2001

não importa qual data seja a maior ou a menor, e a data é validada diretamente da url

